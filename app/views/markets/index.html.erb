<% provide(:title, 'All Markets') %>
<div class="hireus_block">
  <div class="container_12">
    <section class="">
        <div class="row clearfix">
          <div class="grid_10">
            <ul class="blog_pagination">
              <%= render @markets %>
            </ul>       

          </div>
          <div class="grid_2">
            <% if signed_in? && current_user.admin? %>
              <h2><%= link_to 'New Market', new_market_path %></h2>
            <% end %>
          </div>
        </div>
    </section>
  </div>
</div>

